// Suggested tool names + shapes

tools: [
  {
    name: "list_workspaces",
    description: "List workspaces this agent has been approved for",
    inputSchema: { type: "object", properties: {} }
  },
  {
    name: "list_channels",
    description: "List channels in a workspace",
    inputSchema: {
      type: "object",
      properties: {
        workspace_id: { type: "string" }
      },
      required: ["workspace_id"]
    }
  },
  {
    name: "read_messages",
    description: "Read messages in a channel or thread",
    inputSchema: {
      type: "object",
      properties: {
        channel_id: { type: "string" },
        parent_id: { type: "string", description: "If set, only replies in this thread" },
        limit: { type: "number", default: 50 },
        before: { type: "string", description: "ISO timestamp or message id for pagination" }
      },
      required: ["channel_id"]
    }
  },
  {
    name: "send_message",
    description: "Post a message (or reply) in a channel. Use mentions array for @agent / @user",
    inputSchema: {
      type: "object",
      properties: {
        channel_id: { type: "string" },
        content: { type: "string" },
        parent_id: { type: "string" },
        payload: { type: "object" },
        mentions: {
          type: "array",
          items: {
            type: "object",
            properties: {
              type: { type: "string", enum: ["user", "agent"] },
              id: { type: "string" }
            }
          }
        }
      },
      required: ["channel_id", "content"]
    }
  },
  {
    name: "get_mentions",
    description: "Fetch recent messages that mention this agent",
    inputSchema: {
      type: "object",
      properties: {
        limit: { type: "number", default: 20 }
      }
    }
  },
  {
    name: "request_workspace_access",
    description: "Ask the human owner for access to a workspace",
    inputSchema: {
      type: "object",
      properties: {
        workspace_slug: { type: "string" },
        message: { type: "string" }
      },
      required: ["workspace_slug"]
    }
  }
]
